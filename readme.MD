# shell script
**steps:**
* Create repo in Github
* clone repo in local laptop
* start developing 
* add the files in temporary area
'''''''
git add <file name>
''''''''

* commit changes
'''''''''''''''
git commit -m <comment why you commiting >
''''''''''

* Push the chnage to GitHub/Central repo
'''''''''''
git push -u origin main

'''''''''''

**namining convention : use small case with hyphons(-).No spaces**
'''''''''''
we can give or execute all git coomads at a time like below
git add .; git commit -m "some messege"; git push -u origin main 